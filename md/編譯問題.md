1. env SPARK_VERSION=3.5.7 scripts/spark-tests/build-pyspark.sh 有額外裝 softwareupdate --install-rosetta --agree-to-license
2. hatch run scripts/spark-tests/run-server.sh 有 hatch 問題 要以下這樣跑
```
PYO3_PYTHON="/opt/homebrew/bin/python3.11" \
    hatch run test-spark.spark-4.1.1:scripts/spark-tests/run-server.sh
```