# .editorconfig 詳細解說

EditorConfig 是跨編輯器的程式碼風格設定標準，確保團隊成員使用不同編輯器時有一致的格式。

## 完整設定

```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
```

## 設定說明

### 🔸 root = true

告訴編輯器這是專案根目錄的 .editorconfig，不需要往上層目錄繼續尋找

### 🔸 [*] (萬用字元)

套用到所有檔案。也可以針對特定副檔名設定:

```ini
[*.py]
indent_style = space
indent_size = 4

[*.rs]
indent_style = space
indent_size = 4

[Makefile]
indent_style = tab
```

### 🔸 charset = utf-8

檔案編碼使用 UTF-8，這是現代專案標準

### 🔸 end_of_line = lf

行尾字元使用 Unix 風格 LF (`\n`)，而非 Windows 的 CRLF (`\r\n`)

| 值 | 說明 |
|----|------|
| lf | Unix/macOS 風格 |
| crlf | Windows 風格 |
| cr | 舊 Mac 風格 (已少用) |

### 🔸 insert_final_newline = true

檔案結尾自動加入換行字元。這是 POSIX 標準，許多工具預期檔案以換行結尾

### 🔸 trim_trailing_whitespace = true

自動移除行尾多餘空白字元。避免:
- Git diff 雜訊
- 不一致的程式碼風格
- 潛在的合併衝突

## 支援的編輯器

大多數現代編輯器原生支援或有外掛:
- VS Code (內建)
- IntelliJ IDEA (內建)
- Vim (外掛)
- Neovim (外掛)
- Sublime Text (外掛)

## 未設定的選項

Sail 專案未設定但常見的選項:

```ini
# 縮排風格 (space 或 tab)
indent_style = space

# 縮排寬度
indent_size = 4

# 最大行寬 (僅部分編輯器支援)
max_line_length = 100
```

這些由各語言的專用工具處理:
- Rust: rustfmt.toml
- Python: ruff / black
